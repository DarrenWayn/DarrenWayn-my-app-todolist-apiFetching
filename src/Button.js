function Button() {
    // return <button className="btn">{props.children}</button>return <button className="btn">{props.children}</button>
    return <button className="btn">click me</button>
}

export default Button